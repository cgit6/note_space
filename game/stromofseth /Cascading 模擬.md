# Cascading 模擬

## base game 大致流程

盤面生成 -> 消除掉落階段 ->> 算分 -> 獲取贏分符號所引值(依照 payTable) -> 獲取當前盤面上的贏分 -> 先阻擋 TmpWin 更新 -> 判斷是否結束這 round -> false -> 更新這 round 累積贏分 -> 更新乘數狀態(生成新的乘數值、乘數位置、當前累積乘數) -> 判斷是否升級乘數值 -> 消除掉落 -> 補盤

## base game 流程細節

### 盤面生成

首先根據權重設定，選擇某一組輪帶表。利用該組輪帶的設定生成盤面，
