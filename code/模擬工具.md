效能分析 make pprof -> make read-pprof(cpu 的)、make allocs -> make read-allocs(ram 的)、make heap 不怎麼用 -> make read-heap
都要進行 make test 進行測試

## build 資料夾

放執行結果，或是分析結果，要刪除就 make clear

## config 資料夾

放設定檔的地方，檔名 先代號、底線、都小寫名稱

### yaml 檔案

#### 結構

- 遊戲名稱(GameName)
- 押注基本單位 BetUnit
- 最大贏分
- 遊戲模式設定列表，一個遊戲模式相當於一個狀態，遊戲將在各狀態間轉換
- 額外設定(Fixed): 遊戲的特色

#### yaml 細節

## cmd 資料夾

run/main.go 是整個模擬的入口
sim/main.go 是另一個可以 go run . 進行模擬的地方

## Makefile

封裝執行命令

## readme.md

可以看一下執行命令，設計理念什麼的目前不是重點

## internal 資料夾

### internal/spec/enum/gamename.go

### internal/res/game_mode_result.go

儲存整個模擬的結果，將結果放在一個 GameModeResult struct 中

### internal/res/spin_result.go

---

### internal/engine/game/game_registry.go

gameRegister 函數

### internal/engine/game/game_1234_stromofseth.go

1. 都會需要一個 func init 處理
   init 是 Go 的特殊函式名稱。不能有參數、不能有回傳值。在這個 package 被載入時會自動執行一次，而且是在 main() 之前

---

### internal/engine/game/machine.go

## 專案更新

### 12/1 更新

1. yaml 檔案結構，移除 GameSetting layer
2. initFlag 在 machine struct ...
3.
