# 12/19 任務清單

## 代辦清單

- [ ] 修正 `getFreeResult`

先看 Base game 第 4 步 "判斷是否結束消除掉落"

1. 首先，判斷當前盤面是否有贏分符號? 如果有則處理以下步驟
2. 如果 檢視當前盤面 C1 數量
3. 計算 C1 贏分
4. 更新累積贏分
   // 這邊分成兩種情況 "初始盤面"、"消除掉落至盤面" 沒有贏分組合
5. 如果初始盤面沒有贏分組合，則提交 `FinishStep` 提交贏分
6. 如果消除掉落至盤面沒有贏分組合
7. 判斷累積贏分是否達最高贏分
8. 更新 tmp win
   // 後面這邊有兩種可能 "RoundWin" 和 "MaxWin"
9. 提交當前的分數

Free game 結束每 1 round 前的最後一步

特色的累積乘數()

如果 一開始 Screen sym 有值，mult 也有值

NowTotalMult 要在 算分且只能在算分前更新。

從遊戲中可以看到是先計算消除掉落，在消除掉落的過程中更新累積分數，如果有出現乘數符號不顯示，但乘數符號會在消除掉落過程中有概率升級。

- [ ] `"multisymmults": [20, 150, 5, 100, 6, 20]` 有問題啊， 20 怎麼會被重複計算呢?

## 筆記

終端機測試 `make dev 1801`
