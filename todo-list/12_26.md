# 12/23 任務清單

## 帶辦清單

- [ ] 調輪帶

      - BuyFeatureFree 條件需要滿足
      Total RTP 96-97%
      FG RTP : 96-97%
      BG RTP: 0%
      Hit Rate: 29.77%
      Trigger Rate: 0.48%
      CV (STD/RTP) : 待定

      - Normal 條件需要滿足
      total RTP: 96.89% - 96.50%
      FG RTP: 21% 左右
      BG RTP: 大約 75% 左右
      Hit Rate: 1/3.4(29.77%)
      trigger: 1/209.4(0.48%)
      CV (STD/RTP) : 待定

- [x] 輪帶生成方式改成依照權重，目前盤面生成的方式是使用每一格都是獨立的結果，所以我只需要去控制權重參數就行了
- [ ] 可以觀察一下收集到的數據他的權重差異，統計結果的權重差異。我不覺得我自己瞎猜，結果會比較好。

## 進度

## 重點

禮拜三討論完之後我還沒給你 dateline，下禮拜一(12/29) 早上 10:00 跟你討論，完成下面兩件事

1. 將生成盤面的方式改為權重
2. 完成 "一般買入" 的輪帶表設定，目前須滿足以下條件
   total RTP: 96.89% - 96.50%
   FG RTP: 21% 左右
   BG RTP: 大約 75% 左右
   Hit Rate: 1/3.4(29.77%)
   trigger: 1/120(0.8%)
   CV (STD/RTP) : 暫時不考慮

實驗次數需要是 100 萬次以上。

### 生成盤面改成權重

利用權重去生成盤面的設定是 `GenReelType: GenReelBySymbolWeight` 跟 `GenReelType: GenReelByReelIdx` 不一樣。

生成初始盤面沒有 Scatter 但 補盤可能會出現 Scatter

### 搞爬蟲

把畫面縮到超小然後利用自動點擊 canvas 的相對位置進行自動觸發

- 有沒有辦法在 gcp 那邊掛腳本去收 gate 的結果，可能傳到 mongodb 吧，本地 server 只要撈資料去跟 problab 互動就好。
- 真的必須想一個辦法降地效能消耗
- IP 要防禦因為已經跳出機器人驗證了

目標是 1000 萬局

我現在會遇到一個風險，就是即便我獲得了盤面的權重值，我對補盤的機制掌握度也不高(我需要搞明白這補盤的邏輯是什麼?)

[紀錄]初始盤面的權重必然跟補盤的不一樣

[未來方案]這種調輪帶就是多目標的優化問題，你有 5 個組編碼先獲取，如果用優化算法一定可以搞定但還不是時候因為，現在還不知道 base line 在哪裡，所以現在該做的就是把 爬蟲弄好，有了 base line 才能搞優化收斂的思路。雖然還不是時候但是可以想一下，有了 base line 之後才知道收斂的基準，因為會牽涉到使用者體驗的問題。需要對的是
